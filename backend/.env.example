# Supabase
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_ANON_KEY=your_anon_key
SUPABASE_SERVICE_KEY=your_service_role_key

# Encryption key for user API credentials (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
CREDENTIALS_ENCRYPTION_KEY=your_fernet_key

# External APIs (for development/testing only — in production, stored per-user in DB)
SOLAX_TOKEN_ID=your_token
SOLAX_DONGLE_SN=your_sn
TESSIE_API_KEY=your_key
TESSIE_VIN=your_vin

# Ollama
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=qwen2.5:7b

# Telegram (optional)
TELEGRAM_BOT_TOKEN=your_bot_token

# CORS (comma-separated production origins)
ALLOWED_ORIGINS=http://your-vps-ip

# App
CIRCUIT_VOLTAGE=240
POLL_INTERVAL_SECONDS=60
AI_INTERVAL_SECONDS=300
